# Лабораторна робота №8: Робота з JSON-файлами

Веб-застосунок для демонстрації роботи з JSON в Node.js та JavaScript.

## Опис проєкту

Цей проєкт створено як частину лабораторної роботи №8 "Робота з JSON-файлами". Він демонструє:
- Читання та запис JSON-файлів в Node.js
- Серіалізацію та десеріалізацію даних
- Роботу з Fetch API для завантаження даних
- Фільтрацію та обробку JSON-даних
- Злиття кількох JSON-файлів

## Структура проєкту

```
lab8-json-work/
├── data/                          # JSON дані
│   ├── users.json                # Список користувачів
│   ├── products1.json            # Товари (набір 1)
│   ├── products2.json            # Товари (набір 2)
│   ├── products_combined.json   # Об'єднані товари
│   └── user_posts.json          # Пости користувача
├── scripts/                      # Node.js скрипти
│   ├── manageUsers.js           # Управління користувачами
│   ├── mergeProducts.js         # Злиття товарів
│   └── fetchPosts.js            # Завантаження з API
├── fetchPosts.html              # Веб-інтерфейс для завдання 3
├── package.json                 # Конфігурація npm
└── README.md                    # Документація
```

## Завдання

### ✅ Завдання 1: Управління списком користувачів

**Файли:**
- `data/users.json` - JSON файл з користувачами
- `scripts/manageUsers.js` - Node.js скрипт для управління

**Функціональність:**
- Зчитування користувачів з JSON
- Додавання нових користувачів
- Автоматична генерація ID
- Валідація email
- Перевірка на дублікати
- Запис змін назад у файл

**Запуск:**

```bash
# Показати список всіх користувачів
node scripts/manageUsers.js

# Додати нового користувача
node scripts/manageUsers.js "Ім'я Прізвище" email@example.com

# Приклад
node scripts/manageUsers.js "Петро Петренко" petro@example.com
```

**Приклад виводу:**

```
📋 Список всіх користувачів:

ID | Ім'я                    | Email
---+------------------------+--------------------------------
1  | Іван Петренко           | ivan.petrenko@example.com
2  | Марія Коваленко         | maria.kovalenko@example.com
3  | Олександр Шевченко      | oleksandr.shevchenko@example.com

📊 Всього користувачів: 3
```

**Структура даних:**

```json
[
  {
    "id": 1,
    "name": "Іван Петренко",
    "email": "ivan.petrenko@example.com"
  }
]
```

---

### ✅ Завдання 2: Злиття двох JSON-файлів

**Файли:**
- `data/products1.json` - Перший набір товарів
- `data/products2.json` - Другий набір товарів
- `data/products_combined.json` - Результат об'єднання
- `scripts/mergeProducts.js` - Скрипт злиття

**Функціональність:**
- Читання двох JSON файлів
- Об'єднання масивів
- Видалення дублікатів за ID
- Сортування за ціною
- Збереження результату
- Виведення статистики

**Запуск:**

```bash
node scripts/mergeProducts.js
```

**Приклад виводу:**

```
🔄 Об'єднання файлів products1.json та products2.json

📦 Завантажено з products1.json: 5 товарів
📦 Завантажено з products2.json: 6 товарів

🔗 Об'єднано товарів: 9
♻️  Видалено дублікатів: 2

✨ Фінальний список (відсортовано за ціною):

ID | Назва                           | Ціна (грн)
---+---------------------------------+-----------
4  | Клавіатура Logitech             |      1200
5  | Миша Razer                      |      1800
...

📊 Статистика:
   Загальна вартість: 65900 грн
   Середня ціна: 7322.22 грн
   Найдешевший: Клавіатура Logitech (1200 грн)
   Найдорожчий: Ноутбук ASUS (25000 грн)
```

**Структура даних:**

```json
[
  {
    "id": 1,
    "name": "Ноутбук ASUS",
    "price": 25000
  }
]
```

---

### ✅ Завдання 3: Завантаження даних з API

**Файли:**
- `scripts/fetchPosts.js` - Node.js скрипт (використовує https модуль)
- `fetchPosts.html` - Веб-інтерфейс (використовує Fetch API)
- `data/user_posts.json` - Відфільтровані пости

**Функціональність:**
- Завантаження даних з JSONPlaceholder API
- Фільтрація за userId = 5
- Збереження результату в JSON
- Відображення статистики
- Можливість завантаження файлу (в браузері)

**Запуск Node.js скрипта:**

```bash
node scripts/fetchPosts.js
```

**Запуск веб-версії:**

Відкрийте `fetchPosts.html` у браузері. Веб-версія автоматично:
- Завантажує дані з API
- Фільтрує пости
- Відображає їх на сторінці
- Дозволяє зберегти у файл

**Структура даних:**

```json
[
  {
    "userId": 5,
    "id": 41,
    "title": "non est facere",
    "body": "molestias id nostrum..."
  }
]
```

---

## Теоретична частина

### JSON (JavaScript Object Notation)

**Що таке JSON?**
- Текстовий формат для структурованих даних
- Базується на синтаксисі об'єктів JavaScript
- MIME-тип: `application/json`
- Розширення файлу: `.json`

**Основні правила:**
- Ключі в подвійних лапках: `"key"`
- Рядки в подвійних лапках: `"value"`
- Числа без лапок: `42`, `3.14`
- Булеві значення: `true`, `false`
- Null: `null`
- Масиви: `[1, 2, 3]`
- Об'єкти: `{"key": "value"}`
- Немає коментарів
- Немає зайвих ком

### Методи роботи з JSON

#### JSON.parse()

Перетворення JSON-рядка в об'єкт JavaScript:

```javascript
const jsonString = '{"name":"Іван","age":25}';
const obj = JSON.parse(jsonString);
console.log(obj.name); // "Іван"
```

#### JSON.stringify()

Перетворення об'єкта JavaScript в JSON-рядок:

```javascript
const obj = { name: "Марія", age: 30 };
const json = JSON.stringify(obj);
console.log(json); // '{"name":"Марія","age":30}'

// З форматуванням
const formatted = JSON.stringify(obj, null, 2);
```

### Робота з файлами в Node.js

#### Читання JSON файлу

```javascript
const fs = require('fs');

// Синхронне читання
const data = fs.readFileSync('data.json', 'utf8');
const obj = JSON.parse(data);

// Асинхронне читання
fs.readFile('data.json', 'utf8', (err, data) => {
  if (err) throw err;
  const obj = JSON.parse(data);
});
```

#### Запис JSON файлу

```javascript
const fs = require('fs');
const obj = { name: "Тарас", age: 28 };

// Синхронний запис
fs.writeFileSync('data.json', JSON.stringify(obj, null, 2));

// Асинхронний запис
fs.writeFile('data.json', JSON.stringify(obj, null, 2), (err) => {
  if (err) throw err;
  console.log('Збережено!');
});
```

## Контрольні запитання

1. **Що таке JSON та для чого він використовується?**
   - Текстовий формат для обміну даними між клієнтом і сервером

2. **Чим відрізняється JSON від звичайних об'єктів JavaScript?**
   - JSON - текстовий формат, ключі в лапках, обмеження по типах

3. **Які методи використовуються для перетворення між об'єктами та JSON?**
   - `JSON.parse()` - з рядка в об'єкт
   - `JSON.stringify()` - з об'єкта в рядок

4. **Чому у JSON не можна використовувати функції чи undefined?**
   - JSON підтримує лише прості типи даних для сумісності з різними мовами

5. **Різниця між fs.readFile() та fs.readFileSync()?**
   - `readFile()` - асинхронний, не блокує виконання
   - `readFileSync()` - синхронний, блокує до завершення

6. **Що відбувається при зайвій комі або одинарних лапках в JSON?**
   - Помилка `SyntaxError` при парсингу

7. **Роль параметра space у JSON.stringify()?**
   - Форматування виводу з відступами для читабельності

8. **Чим небезпечне використання require() для JSON що змінюються?**
   - require() кешує результат, не бачить змін

9. **Як обробляти помилки при десеріалізації JSON?**
   - Використовувати try-catch блоки навколо JSON.parse()

10. **Переваги та недоліки зберігання даних у JSON?**
    - ✅ Простота, читабельність, підтримка мовами
    - ❌ Розмір файлів, відсутність типізації, не для складних структур

## Технології

- **Node.js 18+** - серверне середовище
- **JavaScript ES6+** - мова програмування
- **Fetch API** - завантаження даних (браузер)
- **HTTPS модуль** - завантаження даних (Node.js)
- **File System (fs)** - робота з файлами
- **JSON** - формат даних

## Автор

Виконано як лабораторна робота №8 з курсу ООРВЗ

## Ліцензія

MIT
