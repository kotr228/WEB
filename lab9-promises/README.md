# Лабораторна робота №9: Обробка промісів та промісіфікація

Повноцінний веб-застосунок на Node.js + Express для демонстрації роботи з промісами та промісіфікації.

## Опис проєкту

Цей проєкт створено як частину лабораторної роботи №9 "Обробка промісів. Промісіфікація на практиці". Веб-застосунок демонструє:
- REST API для роботи з промісами
- Використання `Promise.all()` для паралельного виконання асинхронних операцій
- Ланцюжки промісів (`.then().then().catch().finally()`)
- Промісіфікацію callback-функцій (ручна та через `util.promisify()`)
- Обробку помилок в асинхронному коді
- Комбінатори промісів (`Promise.all()`, `Promise.allSettled()`)
- Single Page Application з вкладками для кожного завдання

## Структура проєкту

```
lab9-promises/
├── public/                       # Статичні файли
│   ├── index.html               # Головна сторінка (SPA)
│   ├── css/
│   │   └── style.css           # Стилі додатку
│   └── js/
│       └── app.js              # Клієнтський JavaScript
├── task1-parallel-fetch.js      # Додатковий CLI скрипт
├── task2-file-chain.js          # Додатковий CLI скрипт
├── task3-promisify-delay.js     # Додатковий CLI скрипт
├── task4-promisify-multi.js     # Додатковий CLI скрипт
├── server.js                     # Express сервер з REST API
├── package.json                 # Конфігурація та залежності
└── README.md                    # Документація
```

## Швидкий старт

### Встановлення залежностей

```bash
npm install
```

### Запуск сервера

```bash
npm start
# або
node server.js
```

Сервер запуститься на `http://localhost:3001`

### Використання

Відкрийте браузер та перейдіть за адресою:
```
http://localhost:3001
```

Ви побачите веб-інтерфейс з чотирма вкладками для кожного завдання.

### Додаткові CLI скрипти

```bash
# Запуск окремих завдань через термінал
npm run task1  # Завдання 1
npm run task2  # Завдання 2
npm run task3  # Завдання 3
npm run task4  # Завдання 4
```

## Завдання

### ✅ Завдання 1: Паралельне завантаження даних (Promise.all)

**Файл:** `task1-parallel-fetch.js`

**Опис:**
Функція `fetchUserAndPosts(userId)` паралельно завантажує дані про користувача та його пости з JSONPlaceholder API.

**Функціональність:**
- Паралельні HTTP GET запити:
  - `GET /users/{userId}` - дані користувача
  - `GET /posts?userId={userId}` - пости користувача
- Використання `Promise.all()` для синхронізації запитів
- Обробка помилок мережі та парсингу JSON
- Повернення об'єкта `{user, posts}`

**Запуск:**
```bash
npm run task1
```

**Приклад виводу:**
```
📡 Завантаження даних для користувача #1...
   GET https://jsonplaceholder.typicode.com/users/1
   GET https://jsonplaceholder.typicode.com/posts?userId=1
✅ Дані успішно завантажено!

📊 Результат:
   Користувач: Leanne Graham (Sincere@april.biz)
   Компанія: Romaguera-Crona
   Кількість постів: 10
```

**Технічні деталі:**
- Використання нативного HTTPS модуля Node.js
- Promise-обгортка для HTTPS запитів
- Обробка помилок на кожному етапі

---

### ✅ Завдання 2: Ланцюжок промісів для роботи з файлами

**Файл:** `task2-file-chain.js`

**Опис:**
Асинхронне читання файлу, перетворення тексту у верхній регістр та запис результату в новий файл.

**Функціональність:**
- Читання `input.txt` за допомогою `fs.promises.readFile()`
- Перетворення тексту: `data.toUpperCase()`
- Запис у `output.txt` за допомогою `fs.promises.writeFile()`
- Ланцюжок: `.then().then().then().catch().finally()`

**Запуск:**
```bash
npm run task2
```

**Приклад виводу:**
```
📖 Крок 1: Читання файлу "input.txt"...
✅ Файл прочитано успішно!
   Розмір: 185 символів

🔄 Крок 2: Перетворення тексту у ВЕРХНІЙ РЕГІСТР...
✅ Текст перетворено!

💾 Крок 3: Запис результату у "output.txt"...
✅ Файл записано успішно!

🏁 Операція завершена!
```

**Технічні деталі:**
- Використання `fs.promises` API
- Обробка помилок через `.catch()`
- Фіналізація через `.finally()`

---

### ✅ Завдання 3: Промісіфікація функції delay

**Файл:** `task3-promisify-delay.js`

**Опис:**
Перетворення callback-функції `delay(ms, callback)` у promise-орієнтовану версію двома способами.

**Функціональність:**
- **Ручна промісіфікація:** функція `delayPromise(ms)`
- **Автоматична промісіфікація:** через `util.promisify()`
- Функція `printWithDelay(text, ms)` для демонстрації
- Порівняння паралельного та послідовного виконання

**Запуск:**
```bash
npm run task3
```

**Приклад виводу:**
```
📝 Тест 1: Ручна промісіфікація (delayPromise)
⏳ Очікування 1000мс перед виведенням...
✅ Перше повідомлення (затримка 1000мс)

📝 Тест 3: Паралельне виконання затримок (Promise.all)
⏳ Запуск 3 паралельних затримок...
   ✅ Затримка 600мс завершена
   ✅ Затримка 800мс завершена
   ✅ Затримка 1000мс завершена
   🏁 Усі затримки завершені за 1005мс
   💡 Паралельне виконання: очікуваний час ≈ 1000мс
```

**Технічні деталі:**
- Обгортка `setTimeout()` в проміс
- Використання `util.promisify()` для автоматичної промісіфікації
- Демонстрація переваг паралельного виконання

---

### ✅ Завдання 4: Промісіфікація з кількома результатами

**Файл:** `task4-promisify-multi.js`

**Опис:**
Створення універсальної функції `promisifyMulti(fn)` для промісіфікації callback-функцій, що повертають кілька значень.

**Функціональність:**
- Оригінальна функція: `calculate(a, b, callback)`
  - Callback формату: `(err, sum, product, quotient)`
- Промісіфікована версія: `calculatePromise(a, b)`
  - Повертає: `Promise<[sum, product, quotient]>`
- Обробка помилок (ділення на нуль)
- Використання `Promise.all()` та `Promise.allSettled()`

**Запуск:**
```bash
npm run task4
```

**Приклад виводу:**
```
📝 Тест 1: Обчислення для a=10, b=5
✅ Результати обчислень:
   Сума (a + b): 15
   Добуток (a × b): 50
   Частка (a ÷ b): 2

📝 Тест 4: Обробка помилки (a=10, b=0)
❌ Очікувана помилка оброблена: Ділення на нуль неможливе!

📝 Тест 6: Promise.allSettled (з помилкою)
   ✅ Обчислення 1: Сума=10, Добуток=16, Частка=4
   ❌ Обчислення 2: Ділення на нуль неможливе!
   ✅ Обчислення 3: Сума=9, Добуток=18, Частка=2
```

**Технічні деталі:**
- Універсальна функція промісіфікації
- Використання rest-параметрів (`...results`)
- Демонстрація `Promise.allSettled()` для толерантності до помилок

---

## REST API

### Завдання 1: Паралельне завантаження

| Метод | Endpoint | Опис |
|-------|----------|------|
| GET | `/api/task1/fetch-user/:userId` | Завантажити користувача та пости (Promise.all) |

### Завдання 2: Ланцюжок промісів

| Метод | Endpoint | Опис |
|-------|----------|------|
| POST | `/api/task2/process-text` | Обробити текст (read → transform → write) |
| GET | `/api/task2/read-output` | Прочитати output.txt |

### Завдання 3: Промісіфікація delay

| Метод | Endpoint | Опис |
|-------|----------|------|
| POST | `/api/task3/delay` | Виконати затримку (delayPromise) |
| POST | `/api/task3/parallel-delays` | Паралельні затримки (Promise.all) |

### Завдання 4: Промісіфікація з кількома результатами

| Метод | Endpoint | Опис |
|-------|----------|------|
| POST | `/api/task4/calculate` | Обчислення (sum, product, quotient) |
| POST | `/api/task4/batch-calculate` | Множинні обчислення (Promise.allSettled) |

---

## Веб-інтерфейс

Застосунок має 4 вкладки:

**📡 Завдання 1: Promise.all**
- Вибір User ID (1-10)
- Відображення інформації про користувача
- Список постів користувача
- Статистика

**📝 Завдання 2: Ланцюжок промісів**
- Текстове поле для введення
- Перетворення у верхній регістр
- Порівняння до/після
- Можливість читання output.txt

**⏱️ Завдання 3: Промісіфікація delay**
- Одинична затримка з вибором методу
- Паралельне виконання кількох затримок
- Порівняння часу виконання

**🔢 Завдання 4: Кілька результатів**
- Одне обчислення (a, b)
- Множинні обчислення з обробкою помилок
- Статистика успішних/неуспішних операцій

---

## Теоретична частина

### Проміси (Promises)

**Проміс** — об'єкт, що представляє результат асинхронної операції у майбутньому.

**Три стани промісу:**
1. **pending** (очікування) — початковий стан
2. **fulfilled** (виконано) — операція завершена успішно
3. **rejected** (відхилено) — операція завершена з помилкою

**Створення промісу:**
```javascript
const promise = new Promise((resolve, reject) => {
  // Асинхронна операція
  if (/* успіх */) {
    resolve(value);
  } else {
    reject(error);
  }
});
```

### Методи промісів

#### then()
Приймає callback для обробки успішного результату:
```javascript
promise.then(
  value => { /* успіх */ },
  error => { /* помилка */ }
);
```

#### catch()
Обробляє тільки помилки (скорочення для `then(null, onRejected)`):
```javascript
promise.catch(error => {
  console.error('Помилка:', error);
});
```

#### finally()
Виконується завжди після завершення (успіх чи помилка):
```javascript
promise.finally(() => {
  console.log('Завершено');
});
```

### Комбінатори промісів

#### Promise.all(iterable)
Виконується коли **всі** проміси виконані. Відхиляється при **першій** помилці.
```javascript
Promise.all([promise1, promise2, promise3])
  .then(([result1, result2, result3]) => { /* всі виконані */ })
  .catch(error => { /* хоча б один відхилено */ });
```

#### Promise.allSettled(iterable)
Чекає завершення **всіх** промісів незалежно від результату:
```javascript
Promise.allSettled([promise1, promise2])
  .then(results => {
    results.forEach(result => {
      if (result.status === 'fulfilled') {
        console.log('Значення:', result.value);
      } else {
        console.log('Помилка:', result.reason);
      }
    });
  });
```

#### Promise.race(iterable)
Повертає результат **першого** завершеного промісу:
```javascript
Promise.race([promise1, promise2])
  .then(result => { /* перший виконаний */ });
```

#### Promise.any(iterable)
Виконується при **першому** успішному промісі:
```javascript
Promise.any([promise1, promise2])
  .then(result => { /* перший успішний */ })
  .catch(errors => { /* всі відхилено */ });
```

### Промісіфікація

**Промісіфікація** — перетворення callback-функції в promise-функцію.

**Ручна промісіфікація:**
```javascript
function delayPromise(ms) {
  return new Promise(resolve => {
    setTimeout(resolve, ms);
  });
}
```

**Через util.promisify():**
```javascript
import { promisify } from 'util';

const delayPromise = promisify(delay);
```

**Для функцій з кількома результатами:**
```javascript
function promisifyMulti(fn) {
  return function(...args) {
    return new Promise((resolve, reject) => {
      fn(...args, (err, ...results) => {
        if (err) reject(err);
        else resolve(results);
      });
    });
  };
}
```

## Контрольні запитання

1. **Що таке проміс та які у нього стани?**
   - Об'єкт для асинхронних операцій: pending, fulfilled, rejected

2. **Яка різниця між then() та catch()?**
   - `then()` обробляє успіх та помилки, `catch()` тільки помилки

3. **Для чого використовується finally()?**
   - Виконується завжди після завершення, для фіналізації ресурсів

4. **Різниця між Promise.all() та Promise.allSettled()?**
   - `all()` — відхиляється при першій помилці
   - `allSettled()` — чекає всіх незалежно від результату

5. **Що таке промісіфікація?**
   - Перетворення callback-функції в promise-функцію

6. **Як працює util.promisify()?**
   - Автоматично створює promise-обгортку для callback-функцій

7. **Коли використовувати Promise.all()?**
   - Для паралельного виконання незалежних асинхронних операцій

8. **Як обробляти помилки в ланцюжку промісів?**
   - Через `.catch()` в кінці ланцюжка або в кожному `.then()`

9. **Різниця між async/await та промісами?**
   - async/await — синтаксичний цукор над промісами, більш читабельний

10. **Коли краще використовувати Promise.race()?**
    - Для таймаутів або вибору найшвидшого запиту

## Технології

### Backend
- **Node.js 18+** - серверне середовище
- **Express 4.x** - веб-фреймворк
- **CORS** - підтримка cross-origin запитів
- **HTTPS модуль** - для HTTP запитів до зовнішніх API
- **fs.promises** - Promise API для файлової системи
- **util.promisify** - промісіфікація callback-функцій

### Frontend
- **Vanilla JavaScript ES6+** - клієнтська логіка
- **Fetch API** - HTTP запити до REST API
- **CSS3** - стилізація з градієнтами та анімаціями
- **HTML5** - семантична розмітка

### Архітектура
- **REST API** - стандартизований інтерфейс
- **SPA (Single Page Application)** - клієнт на вкладках
- **ES Modules** - система модулів (`type: "module"`)
- **Promise** - асинхронне програмування

## Автор

Виконано як лабораторна робота №9 з курсу ООРВЗ

## Ліцензія

MIT
